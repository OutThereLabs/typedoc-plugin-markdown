{{#compact}}
{{#if this}}
  {{#if elementType}}
    {{#with elementType}}
      {{#if types}}({{/if}}
      {{> typeName}}
      {{#if types}}){{/if}}
      []
    {{/with}}
  {{else}}
    {{#if types}}
      {{#each types}}
        {{#if @index}}
          {{#ifCond ../type '==' 'intersection'}}{{else}}⎮{{/ifCond}}
        {{/if}}
        {{> typeName}}
      {{/each}}
    {{else}}
      {{#if elements}}
        [{{#each elements}}{{#if @index}},{{/if}}{{> typeName}}{{/each}}]
      {{else}}
        {{#if name}}
          {{name}}
        {{else}}
          {{#if value}}
            "{{value}}"
          {{else}}
            {{this}}
          {{/if}}
        {{/if}}
        {{#if typeArguments}}
          {{#each typeArguments}}
            {{#if @index}},{{/if}}
            <{{> typeName}}>
          {{/each}}
        {{/if}}
      {{/if}}
    {{/if}}
  {{/if}}
{{else}}
  void
{{/if}}
{{/compact}}
