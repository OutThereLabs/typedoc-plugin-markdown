{{#compact}}
{{#if reflection}}
  [{{{reflection.name}}}](#{{reflection.anchor}})
  {{#if typeArguments}}
    {{#each typeArguments}}
      {{#if @index}}, {{/if}}
      {{> typeName}}
    {{/each}}
  {{/if}}
{{else}}
  `{{> typeName}}`
{{/if}}
{{/compact}}
